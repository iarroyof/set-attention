# Set-Dictionary Branch Status Tracker

Last updated: 2026-07-17 after verifying GitHub push state and auditing Blue/Lizmark checkout safety.

Scope: current experimental state for `set-dictionary/anchor-span`.

Current copy/provenance guard: see
`audit/server_copy_provenance_20260709.md`. As of the 2026-07-17 audit, GitHub
is the authoritative source for new code syncs:

- `set-dictionary/anchor-span` at `3e7edb4`
- `mrp-lca-cmp-sd` at `d0f7ae8` (hosts carry `2ded5d1` via verified bundle,
  unpushed)

The local workspace at `/mnt/d/userfolders/documents/github/set-attention` is
clean on `mrp-lca-cmp-sd` at `2ded5d1`, one commit ahead of
`origin/mrp-lca-cmp-sd`. The earlier `mrp-lca-cmp` branch was a false start
from `origin/main`; do not launch or pull it for current set-dictionary work.

The 2026-07-17 host repair is complete: both `~/set-attention` copies are
clean, compose-validated git checkouts on `mrp-lca-cmp-sd@2ded5d1`; pre-repair
trees are archived under `~/repo_audit_copies/` on each host. Lizmark compose
launches must set the `UID`/`GID` environment. Resolution details:
`audit/incident_branch_host_context_20260717.md`.

Alternate runtime directories such as `~/set-attention-anchor-span-sync` and
`~/set-attention-mrp0-validation` are deprecated audit copies and must not be
used for new launches. Historical rows below may still name the alternate
directory when that was the true launch provenance for completed jobs.

Shared-tracker write owner: current program-integration/status worker during
post-MRP-1 plan alignment. Concurrent task agents update their own
subplans/audits and submit a handoff; they do not edit this file unless
explicitly assigned.

## Canonical precedence

Read these files in order for current work:

1. `docs/set_dictionary_research_main_plan.md` for program scope and dependencies
2. this tracker for live state
3. the assigned task under `docs/agent_plans/`
4. `docs/sd_dense_paper5_matrix.md` for active MRP-1 cells
5. `docs/set_dictionary_dev_agent_prompt.md` for standing implementation guards
6. `docs/set_dictionary_model_provenance_for_math_agent.md` for current architecture/math provenance

`audit/phase_sd_status_legacy_through_20260625.md` and
`audit/SD_9_7_handoff_landmark_legacy_20260625.md` are provenance snapshots, not launch guidance.

Documentation audit: `audit/canonical_documentation_audit_20260630.md`. The local workspace is
canonical. Final propagation of the archive layout/latest wording to remote mirrors is pending; remote
agents must not prefer their local prose over this tracker/matrix.

## Active policy

- The active matrix is **exact dense only**: dense set-dictionary versus matched dense token attention.
- Coverage-scaled landmark (`landmark_coverage=0.25`) is removed from the active matrix. Its completed
  artifacts are historical quality/reference evidence only and must not support linear/sub-quadratic
  efficiency claims.
- Do not launch landmark, sparse, fixed-k, SD-10a, or SD-11 work without a new explicit user decision.
- Full WikiText-2 only: no `data.limit`; 10 epochs; applied RNG seeds 0--4 for corrected trainable paper cells; `D=384`,
  `d_ff=1536`, 6 layers, 8 heads, LR `1e-4`, warmup metadata `1000`.
- Regular paper blur rows: `{b0,b25,b50,b75,b100}`. Existing b62 rows are exploratory and excluded
  from the five-seed queue.
- Set guards: exact backend, `output_residual_mode=anchor_span`, `token_mlp.enabled=false`,
  `anchor.enabled=false`, CE-only, `candidate_fiber=endpoint_window`, no re-read/all_past/multivector.
- Corrected rows require `training.experiment_contract=sd_grid_seeded_v1`,
  `training.diagnostics_contract=current_matrix_v1`, applied seed equality,
  and complete family-specific diagnostics.
- After a detached launch, perform one compact health check and stop polling until the user requests
  another status check.

## Registered continuation program

The post-matrix research program is now canonical at
`docs/set_dictionary_research_main_plan.md`. It divides work into restart-safe
subplans under `docs/agent_plans/`:

| Workstream | State | Operational rule |
|---|---|---|
| MRP-0 reproducibility/checkpoint platform | PASS | Blue container validation passed on 2026-07-07: focused suite, duplicate strict token/b25 smokes, checkpoint replay, and eval-only immutability |
| MRP-1 five-seed exact-dense closure | PASS | Blue 120/120 and Lizmark 135/135 endpoint-valid; strict scan accepted 255 CSVs; later short-B3 bridge added 60/60 descriptive endpoint-valid rows; `b*=b25` frozen |
| MRP-2 natural AR-hit evaluation | APPROVED; CHECKPOINT RETRAINING COMPLETE; EVAL PENDING | user approval recorded 2026-07-08; registered 12-cell checkpoint retraining completed on blue-demon before the post-shutdown audit |
| MRP-3 synthetic MQAR | COMPLETE; NULL/INCONCLUSIVE | all 18 registered rows completed and summarized; frozen b25 accuracy mean 0.0002474, below the 0.90 support threshold, so no specialization claim |
| MRP-4 larger scale separation | NOT_TRIGGERED | MRP-3 failed support condition 3; do not launch scale-separation rows from the current MQAR result |
| MRP-lca-cmp long-context aggregation comparison | ACTIVE; batching runtime validation passed; LCA generator pending | separate compressed-aggregation branch after MQAR null result; exact dense only; Lizmark container guard passed for grad accumulation/eval microbatch preservation |
| MRP-5 tokenizer-matched WT2/PG-19 | BLOCKED | requires MRP-2 completion and explicit transfer approval; MRP-3 review is complete but null |
| MRP-6 theory/proofs | PASS for analytic theory package | MRP-6A/B/C focused tests passed in Blue container; MRP-6D canonical TeX integration and clean build passed; MRP-3 completed null, so empirical specialization remains unestablished |
| MRP-7A current-evidence paper rewrite | ACTIVE; no new experiments | MRP-1, short-B3 bridge, and MRP-6D support Results/table/figure cleanup; no AR/MQAR/PG-19 final claims |
| MRP-7 paper synthesis | PARTIAL DRAFTING ALLOWED; FINAL BLOCKED | MRP-1, MRP-3-null, MRP-4-not-triggered, and MRP-6D are available; final synthesis still waits for MRP-2/5 |

No new experimental queue is authorized by the plan alone.

## Open reproducibility incident

`audit/incident_training_seed_not_applied_20260630.md` records that the legacy
runner logged `training.seed` without applying it. All legacy MRP-1 rows are
unpaired stochastic replicates, not reproducible seeds.

The runner now applies the configured seed before data/model construction and
logs requested/applied/torch seeds plus deterministic and benchmark flags.
The isolated corrected namespace refuses completion without matching
provenance. The seed/config/diagnostics patch is deployed on both experiment
hosts. At the time of the corrected MRP-1 launch, full MRP-0 remained
incomplete because checkpoint, loader-batch, and data/tokenizer provenance
work had not been implemented.

The 2026-07-06 implementation audit confirmed this was not merely pending
validation at launch time: the checkpoint and ordered-token modules did not exist, no
corrected MRP-1 checkpoints were saved, and all 120 Blue logs warn that
CuBLAS execution was not fully deterministic. This does not erase the applied
five-seed PPL/VRAM matrix, but exact replay and downstream checkpoint reuse are
not supported. See `audit/incident_mrp0_prelaunch_gap_20260706.md`.

The missing platform has since been implemented and validated in the Blue
container. No selected retraining is authorized without the registered
one-step full-shape preflight for that new stage. Audit:
`audit/MRP_0_reproducibility_platform.md`.

## Grid identity and diagnostics incident

`audit/incident_sd_grid_identity_diagnostics_20260701.md` records scanner
conflation, missing per-group training diagnostics, and the strict-contract LR
type mismatch. No duplicate successful result was found. Blue partial/fail-fast
attempts and superseded retry records are outside aggregation roots; see
`audit/SD_grid_duplicate_cleanup_20260701.md`.

The final blue protocol now:

- validates set and token configs through the real runner before workers start;
- rejects every non-null data limit and wrong implementation/config;
- logs fine/coarse routing, pooling, router parameter/gradient, set-stack
  gradient, ablation, effective-range, and rarity/position diagnostics;
- fails corrected aggregation on duplicate cell IDs or missing diagnostics.

The local launcher additionally stops on the first contract error. It must not
be synchronized into active blue/lizmark source; deploy after each host is idle.

## Completed architecture ladder

| Range | Status | Current interpretation |
|---|---|---|
| SD-1--SD-4 | DONE | Causality, leakage, fairness, and config contracts validated. |
| SD-5--SD-8.1 | DONE | Capacity/anchor-span ladder validated; historical details remain in the archived tracker and per-task audits. |
| SD-9 short dense | DONE | Multi-resolution set-vs-set signal established at `L=512,B=16`; 25% blur beats all-fine on PPL and VRAM. |
| SD-9/9.5/9.6 landmark arms | HISTORICAL | Preserve artifacts, but exclude from the active dense claim and from linear-efficiency conclusions. |
| SD-10a/SD-11 | HOLD | No architecture changes until the dense matched comparison is complete and reviewed. |

## Completed primary three-seed matrix

Active scheduler: `scripts/run_sd_grid.sh`.
Canonical scanner: `scripts/sd_grid_status.py`.

| Island | Dense set status | Dense token status |
|---|---|---|
| exact, `L=512,B=16` | six blur rows, 3/3 seeds complete | seeds 0--2 complete at registered epoch 10 |
| exact, `L=512,B=4` | six blur rows, 3/3 seeds complete | seeds 0--2 complete |
| exact, `L=1024,B=4` | six blur rows, 3/3 seeds complete | seeds 0--2 complete |
| exact, `L=2048,B=4` | six blur rows, 3/3 seeds complete | seeds 0--2 complete |
| exact, `L=4096,B=4` | b50/b62/b75/b100 complete; b0/b25 OOM 3/3 | OOM 3/3 before epoch 1 |

Dense set endpoint: 84/90 planned cells completed and 6/90 resolved as OOM. The set sweep is therefore
fully resolved. Through `L=2048`, b25 is the lowest-PPL row and also uses less VRAM than b0. At `L=4096`,
b0 and b25 do not fit on 49 GiB; b50 is the lowest-PPL feasible row.

## Primary job resolution

The primary corrected queues ended and were validated:

- Blue PID `3489171`: queue ended normally; all 8 requested cells completed 10/10.
- Lizmark PID `2153424`: queue ended normally; L2048 completed 3/3 and L4096 OOMed 3/3.
- Corrected artifacts/logs were pulled locally and validated.
- Result audit: `audit/SD_dense_matched_results.md`.

## Configuration incident

The first exact-token attempts failed before model construction because the scheduler used the landmark
token YAML and attempted to clear inherited `backend_params` with `{}`. Exact validation correctly
rejected the nonempty parameters.

Resolution:

- exact token cells now use `configs/paper_lr_norm/baseline_dense_exact.yaml`;
- landmark and sparse token configs are selected only for those explicit backends;
- in-flight duplicate detection uses the same backend-native token config signature;
- `scripts/sd_grid_status.py` reports the registered target epoch (10 by default), so a reusable 30-epoch
  run contributes its epoch-10 row rather than its final row;
- container config checks and token-only dry runs passed on both hosts.

Impact: no set result was affected. The corrected controls are now complete and establish the matched
verdict in `audit/SD_dense_matched_results.md`.

Incident: `audit/incident_sd_dense_token_exact_backend_params.md`.

## Frontier extension resolution

The seed-0 full-data frontier extension tested the strong L4096/B4 b50 result with an overlap
rectangle that separates length and native batch:

- blue: L2048/B3 full blur + token;
- lizmark: L3584/B4, L3584/B3, and L4096/B3 full blur + token;
- B2 is fallback only if B3 remains unsupported.

See `docs/sd_dense_matched_comparison_plan.md`. Do not reopen landmark or add
B4 OOM retries to the active matrix.

Final frontier state:

| Host | PID | Cells | Log | Result |
|---|---:|---|---|---|
| blue-demon | 3657887 | L2048/B3 full blur + token, seed 0 | `logs/sd_grid_blue_frontier_seed0.log` | DONE 7/7, exit 0, validated and pulled |
| lizmark | 2185668 | L3584/B4, L3584/B3, L4096/B3 full blur + token, seed 0 | `logs/sd_grid_lizmark_frontier_seed0.log` | DONE 21/21, exit 0, validated and pulled |

All 28 cells use full data and 10 epochs. Validation found no guard, metadata, peak-VRAM, NaN/Inf,
traceback, or unexpected OOM failure. Both hosts and all four GPUs are idle. B2 remains unlaunched.

Seed-0 selected results:

- L2048/B3: b25 `900.751/13797.1 MiB`; token `885.786/14189.7 MiB`.
- L3584/B4: b25 `846.937/38552.9 MiB`; token `890.875/41076.2 MiB`.
- L3584/B3: b62 `915.146/24467.7 MiB`; token `869.081/31035.0 MiB`.
- L4096/B3: b25 `840.564/35350.1 MiB`; token `969.223/37955.4 MiB`.

Native B3 changes optimization and remains a separate island. The two seed-0 Pareto results are
promising but not conclusive. Audit: `audit/SD_dense_frontier_extension.md`.

## Five-seed paper matrix

The 2026-06-30 queues are retained as legacy unseeded-replicate evidence and
have ended. Corrected queues are active on both hosts.

The corrected matrix reruns every supported row with actual seeds `0..4`.
Every supported island uses set rows `{b0,b25,b50,b75,b100}` plus exact token;
b62 remains outside the regular matrix.

| Host | Driver PID | Corrected cells | Queue log | State |
|---|---:|---:|---|---|
| blue-demon | exited normally | 120/120 endpoint-valid done | `logs/sd_grid_blue_paper5_seeded_v1.log` | COMPLETE and locally validated; both GPUs idle |
| lizmark replacement | exited normally | 15/15 L3584/B4 mixed replacement rows endpoint-valid; Lizmark total 135/135 valid | `logs/sd_grid_lizmark_paper5_seeded_v1_replacements_20260707.log` | COMPLETE; both GPUs idle |

The 2026-07-08 Lizmark check found no active grid driver or training process;
both RTX 6000 Ada GPUs reported 1 MiB used and 0% utilization. The queue log
ends with `=== SD-GRID lizmark complete ===`. Artifacts were pulled locally.
The strict scanner accepted all 255 corrected endpoint CSVs under
`SD_GRID_REQUIRE_CONTRACT=sd_grid_seeded_v1`. Both corrected OOM registries are
header-only.

Final summary artifacts:

- `audit/SD_dense_paper5_final_20260708.md`
- `out/paper_integrated_evidence/checks/sd_grid_seeded_v1_final_20260708/`
- `out/final_paper_bundle/overleaf_ready/tables/sd_grid_final_matrix.tex`

MRP-1 paper5 is closed. A later short-B3 bridge extension was launched on
2026-07-10 to fill the missing `L512/B3` and `L1024/B3` islands for the full
allocation picture; this does not reopen landmark experiments or change the
closed paper5 evidence bundle.

## Short B3 Bridge Extension

On 2026-07-10, blue-demon was not reachable from the local workspace
(`ssh`: no route to host). Lizmark was reachable at
`~/set-attention-anchor-span-sync`, both RTX 6000 Ada GPUs were idle, and the
active source copy is a deployed tree rather than a git checkout.

The missing `L512/B3` and `L1024/B3` exact-dense bridge islands use
`GRID_PROFILE=short_b3`, `GRID_NAMESPACE=sd_grid_seeded_v1`,
`RUN_TAG=seeded_v1`, seeds `0..4`, `REQUIRE_APPLIED_SEED=1`, and
`TRAINING_DETERMINISTIC=true`. The profile contains only token plus
`b0/b25/b50/b75/b100` for those two islands: 60 cells total. The first launch
correctly failed fast because the strict `sd_grid_seeded_v1` contract had not
yet admitted `(L,B)=(512,3)` and `(1024,3)`. The contract and scheduler were
updated, synced to Lizmark and blue-demon, the fatal markers were cleared, and
host-scoped dry-runs reported exactly 30 planned cells per host after the
split.

Initial launch used one worker per GPU: Lizmark driver PID `458789`, worker
PIDs `459198` and `459199`; queue log
`logs/sd_grid_seeded_v1/lizmark/short_b3_20260710_201638.log`. Initial health
check showed both first cells running (`L512/B3 b0`, seeds 0 and 1) with about
4.4 GiB used per GPU.

The short-B3 rows are small relative to Lizmark memory. Completed reference
peaks are `L512/B4 <= 4138 MiB` and `L1024/B4 <= 8302 MiB`; four concurrent
`L1024/B3` rows remain below 34 GiB before scheduler margin on a 49 GiB GPU.
On user request, the scheduler was extended with `GPU_WORKERS_PER_DEVICE` and
the queue was initially relaunched in accelerated co-resident mode on Lizmark.
When blue-demon became reachable, the all-length Lizmark queue was stopped and
the bridge was split by length:

| Host | Share | Launch | Current health |
|---|---|---|---|
| blue-demon `~/set-attention-anchor-span-sync` | `L1024/B3`, token + `b0/b25/b50/b75/b100`, seeds `0..4` | parent PID `16795`, driver PID `16798`, workers `17133 17134 17135 17136`; log `logs/sd_grid_seeded_v1/blue/short_b3_L1024_slots2_20260710_203947.log`; `GPU_WORKERS_PER_DEVICE=2` | first split launch failed before training because `.hf/datasets` lacked the offline WikiText cache; cache copied from Lizmark, relaunch passed cache load, four containers live, both GPUs sampled at 99% utilization |
| lizmark `~/set-attention-anchor-span-sync` | `L512/B3`, token + `b0/b25/b50/b75/b100`, seeds `0..4` | parent PID `467354`, driver PID `467357`, workers `467772 467773 467774 467775 467776 467777 467778 467779`; log `logs/sd_grid_seeded_v1/lizmark/short_b3_L512_slots4_20260710_204041.log`; `GPU_WORKERS_PER_DEVICE=4` | eight containers live, CUDA visible inside containers, CSV headers written; host `nvidia-smi` initially sampled 0% while jobs were still in startup/preprocessing |

Treat this bridge extension as accelerated PPL/per-process-peak evidence, not
as exclusive-capacity or OOM evidence.

Completion audit on 2026-07-13: both GPU servers were idle, with no `sdgrid`,
MRP, `run_experiment.py`, or `run_mqar.py` processes. The corrected endpoint
validation includes token rows under `token/L*` and set rows under `set/L*`.
Both short-B3 islands are complete: `L512/B3` has 30/30 endpoint-valid rows on
Lizmark, and `L1024/B3` has 30/30 endpoint-valid rows on blue-demon. Both OOM
registries and contention-OOM registries are header-only. Audit:
`audit/SD_short_b3_bridge_20260713.md`.

On 2026-07-11, blue-demon still had pending `L1024/B3` cells but one worker
was admission-deferred because the running scheduler refuses to add a second
job to a GPU once it sees an existing grid process. To improve occupancy
without restarting and losing partial progress, three low-memory all-coarse
auxiliary cells were launched under the same lock/marker/output protocol:
`b100` seeds 2, 3, and 4, with seed 2 on GPU1, seed 3 on GPU0, and seed 4 on
GPU1. Wrapper logs are
`logs/sd_grid_seeded_v1/blue/aux_b100_L1024_b3_seed{2,3,4}_gpu*.wrapper.log`.
Post-warm-up memory was about 19.6 GiB on each 24 GiB GPU; do not add another
auxiliary cell without a fresh VRAM check.

The scheduler admission bug was then fixed in `scripts/run_sd_grid.sh`: future
drivers no longer defer merely because a grid process is already visible on a
GPU; they proceed to the existing free-VRAM estimate plus margin/headroom
check when `ALLOW_GPU_CORESIDENCY=1`. The fixed script was synced to both
blue-demon and Lizmark. Running drivers keep their original shell state, so
auxiliary locked cells remain the safe way to improve occupancy until those
drivers exit.

Lizmark had the same under-fill pattern on 2026-07-11. With the running
`L512/B3` queue using only about 12.7 GiB per 49 GiB GPU, eight auxiliary
cells were launched under the same lock/marker/output protocol: `b100` seeds
0-4 and `b75` seeds 0-2, split across GPUs. Wrapper logs are
`logs/sd_grid_seeded_v1/lizmark/aux_{b100,b75}_L512_b3_seed*_gpu*.wrapper.log`.
Post-warm-up state was 14 SD containers live, both GPUs at 99% utilization,
and about 15.8 GiB used per 49 GiB GPU. This is still safely below the Lizmark
memory ceiling; do not add another auxiliary wave without a fresh VRAM check.

## MRP-3 MQAR Launch State

User approval for the registered MRP-3 calibration/primary/capacity protocol
was clarified on 2026-07-08. A one-step token calibration preflight completed
on blue-demon for LR candidates `1e-4`, `3e-4`, and `1e-3` under the
validated project container. The first preflight emitted CuBLAS deterministic
warnings, so the real calibration launch sets
`CUBLAS_WORKSPACE_CONFIG=:4096:8`.

Calibration launch state:

| Host | GPU | Container/PID | Row | Log | State |
|---|---:|---:|---|---|---|
| blue-demon `~/set-attention-anchor-span-sync` | 0 | stopped container `4b037ced678e`; docker PID `1930288`, python PID `1930444` exited | token calibration LR sweep, stopped during first `lr=1e-4` row | `logs/mrp3_mqar_calibration_token_20260708_172459.log` | STOPPED/QUARANTINED; endpoint-only cadence did not satisfy registered calibration protocol |
| blue-demon `~/set-attention-anchor-span-sync` | 0 | container `14948b72d7a0`; docker PID `1945245`, python PID `1945389` exited | token calibration LR sweep: `lr=1e-4` failed, `3e-4` passed at 14000, `1e-3` passed at 12500 | `logs/mrp3_mqar_calibration_token_20260708_173704.log` | COMPLETE; selected `lr=0.001`, update `12500` |
| blue-demon `~/set-attention-anchor-span-sync` | 0 | prior driver/container absent after shutdown | original primary matrix, rows token/b0/b25/b50/b75/b100 seeds 0-2, B4, lr 0.001, 12500 updates | `logs/mrp3_mqar_primary_B4_lr0p001_u12500_20260709_083600.log` | INTERRUPTED after 8/18 rows; completed token/b0/b25/b50/b75/b100 seed 0 plus token/b0 seed 1; `b25_seed1_B4.csv` was a stub with no final checkpoint |
| blue-demon `~/set-attention-anchor-span-sync` | 0 | exited before 2026-07-15 idle check | resumed primary rows seed 1: b25/b50/b75/b100, B4, lr 0.001, 12500 updates | `logs/mrp3_mqar_resume_seed1_remaining_gpu0_20260713.log` | COMPLETE; endpoint rows pulled locally |
| blue-demon `~/set-attention-anchor-span-sync` | 1 | exited before 2026-07-15 idle check | resumed primary rows seed 2: token/b0/b25/b50/b75/b100, B4, lr 0.001, 12500 updates | `logs/mrp3_mqar_resume_seed2_all_gpu1_20260713.log` | COMPLETE; endpoint rows pulled locally |
| lizmark `~/set-attention-anchor-span-sync` | 0/1 | split shell drivers exited | capacity preflight L4096/B4/Dkv512, rows token/b0/b25/b50/b75/b100, seed 0, one update | `logs/mrp3_capacity_L4096_B4_gpu0_20260709_090500.log`, `logs/mrp3_capacity_L4096_B4_gpu1_20260709_090500.log` | COMPLETE; no NaN/Inf, traceback, or OOM |

Calibration is frozen. Do not change the selected `lr=0.001` or
`max_updates=12500` for the primary MQAR rows. Capacity preflight rows are
feasibility/VRAM observations only and must not be interpreted as trained MQAR
quality.

Post-shutdown audit on 2026-07-11 found the active blue-demon short-B3 grid
still running with pending `L1024/B3` cells, including GPU1 work. That block is
now cleared. On 2026-07-13 the remaining MRP-3 primary rows were relaunched on
Blue with the hardened `MQAR_SEEDS`/`MQAR_ROWS` resume wrapper, preserving the
frozen `lr=0.001` and `max_updates=12500`. On 2026-07-15, blue-demon and
lizmark were both idle. The 18 primary endpoint CSVs were pulled locally and
`python scripts/summarize_mqar.py out/mqar_primary_B4_lr0p001_u12500/*.csv
--out out/mqar_primary_B4_lr0p001_u12500/summary.tsv` accepted all rows.
Log scan over the primary and resume logs found no NaN, Inf, traceback, OOM,
error, failed, or permission-denied marker.

MRP-3 result: null/inconclusive. The frozen `b25` row has mean accuracy
`0.0002474`, far below the registered `0.90` support threshold. MRP-4 is
therefore `NOT_TRIGGERED` because the primary task is inadequate for
scale-separation interpretation.

Patch status: `src/train/mqar.py`, `scripts/run_mqar.py`,
`scripts/run_mqar_calibration.sh`, and `src/train/metrics_schema.py` now support
the registered 500-update calibration-eval cadence. Reduced-cadence Blue
container validation passed with two update-indexed rows for each token LR
candidate and explicit calibration gate columns.

## MRP-lca-cmp Calibration Launch State

User approval for the registered MRP-lca-cmp calibration matrix was given on
2026-07-18 (plan `docs/agent_plans/mrp_lca_cmp.md`, matrix
`configs/lca_cmp/matrix.md`). Scope: islands `L=1024,B=4` and `L=2048,B=4`,
families token + set b0/b25/b50/b75/b100, seeds 0/1/2 (36 trained rows),
`D=384 d_ff=1536 6L 8H lr=1e-4`, `grad_accum_steps=1`,
`eval_microbatch_size=null` (native batching; each trained row's
`train/peak_vram_mib` doubles as the registered native-batch memory record).
Calibration budget: `max_updates=2000` (modest learnability check: 8000
samples seen at B=4, well under the 20000-example train split; the gate only
requires token dense to move well above chance and one set row to reach a
nondegenerate regime, not convergence).

Launch-host fixes validated in the blue-demon container before launch:

- `scripts/run_lca_cmp.py` previously logged `training.seed` without applying
  it; it now calls `apply_training_seed` (seed 0 vs 1 one-step preflights
  produce different inits, `training.seed_applied=True` in CSV).
- `src/train/metrics_schema.py` had no `lca_cmp` TASK_METRICS entry, so task
  metrics were silently dropped from the CSV; an `lca_cmp` column set
  (loss/ppl/accuracy, update counters, effective batch, exact-sequence
  accuracy, near-bucket metrics) was added and validated.
- `scripts/run_lca_cmp_calibration.sh` (new driver) is restart-safe
  (skip-on-complete CSV or `.done` marker, stale-lock reclaim), keeps an OOM
  registry (`out/lca_cmp/calibration/oom_registry.tsv`), appends per-row
  results to `calibration_runs_<host>.tsv`, scans each log for
  traceback/OOM and each CSV endpoint for nan/inf before marking done, and
  retries any native-OOM cell once as a labeled microbatch row
  (batch 2 x grad_accum 2, effective batch 4).

Sanity pass on blue-demon 2026-07-18: `py_compile` clean;
`pytest tests/ -k lca -q` 10 passed / 2 skipped; one-step preflights passed
for token (`val_loss=2.2417 val_acc=0.4995`, chance) and set b25
(`val_loss=3.9124`, chance regime). Hosts run `mrp-lca-cmp-sd@2ded5d1`, one
commit behind origin `91cb271`; `git diff 2ded5d1..91cb271` touches only
`audit/`, `docs/`, and `tests/` (no runtime code), so host checkouts are
runtime-equivalent.

Calibration launch state:

| Host | GPU | Container/PID | Row | Log | State |
|---|---:|---|---|---|---|
| blue-demon `~/set-attention` | 0 | driver exited | calibration queue, 36 rows round-robin across both GPUs | `logs/lca_cmp/blue/queue.log`, `logs/lca_cmp/blue/worker_gpu0.log` | COMPLETE 2026-07-18 19:23 |
| blue-demon `~/set-attention` | 1 | same driver, second worker | same queue | `logs/lca_cmp/blue/worker_gpu1.log` | COMPLETE 2026-07-18 19:23 |

Completion state (verified 2026-07-18): 36/36 rows endpoint-valid (exit 0,
complete schema-fixed CSVs, `.done` markers), OOM registry header-only,
strict log scan clean (no traceback/NaN/Inf/OOM), both blue-demon GPUs idle
(1 MiB, 0% util, no `lcacmp_*` containers). Gate outcome: Gate 1 (token
learnability) PASS at L1024, marginal at L2048; **Gate 2 FAIL — all 30 set
rows at chance**. Diagnosis and recommendation (structural `endpoint_window`
receptive-field ceiling; probe `candidate_fiber=all_past` before any
relaunch) in `audit/LCA_calibration_20260718.md`. Per the plan's STOP
condition, no scale rows were launched.

Fiber probe update (2026-07-20): the single approved `all_past` probe
(`b25|L1024|seed0`) is **OOM-censored on both hosts** — native B4 OOM on Blue
(26.97 GiB router score tensor) and Lizmark, and the one registered
microbatch retry (b2 x accum2) also OOMed on Lizmark (~60 GiB total demand).
Root cause: the all_past candidate-gather router materializes an O(L^2)
score tensor. No trained probe row exists; the receptive-field diagnosis
remains the best Gate-2 explanation, and any further probe attempt (batch 1
x accum 4 or a chunked gather) needs a new explicit user decision. Details:
`audit/LCA_calibration_20260718.md` "Fiber Probe Outcome". Both hosts idle.

Router-dense probe update (2026-07-20): the user-approved option-0 probe
(`all_past` + `router.score_mode=dense`, label `allpast_routerdense_probe`)
completed cleanly on Lizmark (peak 3216 MiB native B4 — candidate-gather OOM
was key duplication, not the O(L^2) scores). Reachability was achieved
(candidate_count_max=1023, ~69-96 effective candidates) but the row still
did not learn (val_loss 0.817, val_acc 0.498 vs token 0.77 at the same
budget). **Candidate reachability is not the binding constraint; the Gate-2
cause is deeper than the fiber.** Details: `audit/LCA_calibration_20260718.md`
"Router-Dense Probe Verdict". Both hosts idle.

Mechanistic probe series (2026-07-20): P0-P3 complete on Lizmark, all clean.
P1 full routing (`router_topk=1023`) flips the all-past dense b25 row from
0.498 to **0.827** val_acc — top-k=16 sparse selection was a primary
bottleneck. P2 prefix supervision lifts the set row to **0.787** (token
0.903) — final-token-only supervision was an independent bottleneck. P3
oracle count tokens reach **0.985** — routing/readout is fine; the residual
deficit is pooling/set-state learning. Conclusion: set-path/task mismatch,
NOT inherent architectural disadvantage. Per-update loss-curve logging added
(P0); 21/21 LCA tests pass in container. Details:
`audit/LCA_calibration_20260718.md` "Mechanistic Probe Series". Both hosts
idle.

Combined probe (2026-07-20): `all_past`+dense+`router_topk=1023`+prefix
supervision composes the two fixes — b25/L1024/seed0 reaches **val_acc
0.936, val_loss 0.141**, exceeding token prefix supervision (0.903) at the
same budget (peak 3216 MiB). Single-seed diagnostic label; O(L^2) scores
mean the larger-L memory frontier is unresolved. Follow-up 3-seed L1024
mini calibration (`prefix3_token`, `prefix3_b25_fulltopk`,
`prefix3_b25_topk16`, 9 rows) launched on blue-demon under the new
host-preference rule (≤24 GB work uses Blue's faster 4090s; recorded in
`memory/blue-demon-access.md`). Driver `scripts/run_lca_prefix_3seed.sh`;
results: token prefix 0.9443±0.0317, b25 full-topk 0.8956±0.0407, b25
top-k16 0.8171±0.0114 (9/9 clean, Blue idle). The seed-0 set-over-token gap
was seed noise; robust findings are set-in-token-regime, full-routing
> top-k16 (+0.079 at equal VRAM), no set advantage claimed. Details:
`audit/LCA_calibration_20260718.md`.

Blur frontier sweep (2026-07-24): 12/12 rows clean on Blue. **b75 meets the
target: 0.9233±0.0157 val_acc (within seed noise of token 0.9443±0.0317) at
2346.7 MiB, −12.5% vs token's 2680.6 MiB** — first diagnostic set row
matching token quality at lower VRAM on the LCA task. Quality is
non-monotone in blur (b75 > b50 ≈ b25 > b100): coarse atoms shrink the
O(L^2) score tensor AND are better counting units; all-coarse loses detail.
Host sync after sweep: Blue and Lizmark are both clean git checkouts at the
current origin HEAD; local/GitHub/hosts fully synced. Adaptive host access
wrapper `scripts/sa_ssh.sh` (LAN preferred, Tailscale fallback) is the
required entry point. Next per user staging: top-k bandwidth sweep
{16,32,64,128,256,full} on b25/L1024 prefix, then pooling isolation;
L>=2048 frontier probe needs its own plan amendment.

Top-k bandwidth sweep (2026-07-24, Blue, b75/L1024/prefix, 21/21 clean):
quality rises monotonically with router_topk (16: 0.7836±0.0208 ->
1023/full: 0.9233±0.0157; no plateau) — global aggregation needs routing
bandwidth. Peak VRAM is flat across topk (2347-2408 MiB): in
score_mode=dense the [B,H,T,M] score tensor is materialized regardless,
so top-k sparsity buys no memory in this router mode; sparsity only helps
memory if scoring itself is sparse. topk=1023 replicates the prefixblur
b75 row exactly. TSV `out/lca_cmp/topksweep/topksweep_blue.tsv`.

L2048 pilot (2026-07-24, Lizmark, seed 0, 3/3 clean): token prefix
0.9440 @ 9123.9 MiB; b75 full routing (topk=2047) 0.8158 @ 7201.1 MiB
(**−21% VRAM vs token, memory edge scales with L**); b75 topk256 0.7571
@ 7325.4 MiB. Quality gap widens vs L1024 (−0.128 vs −0.021); token is
flat across L, so the set row degrades. Open: seeds 1-2 (pre-approved
"0-2 if feasible"), 2000-update budget sufficiency at L2048 (check
curves). The "multiscale disabled; using single-scale bank" warning is
resolved benign: the flag is unimplemented and unrelated to the
multiresolution head groups that define b75's fine/coarse split. TSV `out/lca_cmp/l2048pilot/l2048pilot_lizmark.tsv`.
Details: `audit/LCA_calibration_20260718.md`.

L2048 follow-ups (2026-07-26): pilot seeds 1-2 on Blue (6/6 clean;
3-seed @2000upd: token 0.9407±0.0066, b75full 0.8553±0.0483, b75topk256
0.8107±0.0476 — high b75 seed variance = undertraining signature) and a
4000-update budget probe on Lizmark (seed 0 controlled pair): token
saturated (0.9438 vs 0.9440 @2000), **b75full 0.8158 -> 0.9353 (+0.12),
within 0.009 of token at −21% VRAM**. The L2048 gap was primarily
budget, not architecture. Caution recorded: the L1024 bandwidth curve
and sparse rows were all measured @2000upd and may partially reflect
training speed; a budget-matched sparse control (topk256 @4000upd) is
needed before the "aggregation needs bandwidth" claim is a convergence
fact. Driver `scripts/run_lca_l2048_budget.sh`.

L2048 budget-matched confirmation (2026-07-27, Blue+Lizmark, 4000upd,
3/3 clean, pulled and validated): **b75full 3-seed 0.9078±0.0368**
(0.9353/0.8660/0.9221) vs token ≈0.941-0.944 — Pareto claim holds on
average (−21% VRAM, mean gap ~0.033) but the set row is less seed-stable
than token (sd 0.037 vs 0.007). topk256 @4000 seed0 = 0.8099 (was 0.7571
@2000): the bandwidth deficit is BOTH training speed (+0.053 from budget)
AND genuine (−0.125 vs full at matched budget). Full routing dominates
sparse top-k at L2048 since top-k buys no VRAM in dense-score mode.
L4096 admission/frontier probe is the next planned step, pending user
approval of its plan amendment.

L4096 Stage-A admission (2026-07-28, Lizmark, 30 updates, native B4):
**token 33745.8 MiB vs b75full 24910.2 MiB (−26.2%)** — the b75 memory
edge grows with L (−12.5% L1024, −21.1% L2048, −26.2% L4096). Neither
row fits Blue's 24 GB at L4096; scientific rows are Lizmark-only. No OOM
fallback needed. Stage B (token + b75full, seeds 0-2, 4000 updates,
seed 0 first) awaits user confirmation of these numbers. Driver
`scripts/run_lca_l4096_admission.sh`; TSV
`out/lca_cmp/l4096admission/l4096admission_lizmark.tsv`.

L4096 Stage B seed0 (2026-07-28, Lizmark, 4000upd): token 0.9407 @
33745.8 MiB (42 min by launcher/result timestamps, L-insensitive as
before); b75full 0.8382 @ 24915.9 MiB (4h55m by the same source, about
7x slower practical throughput). b75 train loss still descends at
update 4000 (1000-update means 0.5208/0.3591/0.2864/0.2486; −0.038
over the last 1000) — same qualitative undertraining signature as the
L2048 seed0 row that later reversed; no parity-failure conclusion
licensed. Watch item: b75 validation loss remains much worse than
token's, but the generalization-gap conjecture needs the 8000-update
extension or additional diagnostics because the curve does not yet show
a plateau. Proposed next: b75full seed0 @8000upd before seeds 1-2 (user
decision pending).

Host/artifact sync correction (2026-07-26): an earlier report falsely
claimed full sync. Actual state then: local/GitHub at `c4e9b26`, Blue and
Lizmark at `ed91439` with untracked scp'd driver scripts. Corrected: both
hosts fast-forwarded to `c4e9b26` (untracked scripts verified
byte-identical to committed versions before removal; no tracked
modifications on either host), and all missing per-run artifacts pulled
into the local authoritative tree: topksweep 21 rows, l2048pilot 9 rows
(seeds 0-2), l2048budget 2 rows — every TSV row's CSV present and
schema-complete (train/loss, val/loss, val/accuracy, train/peak_vram_mib)
under `out/lca_cmp/{topksweep,l2048pilot,l2048budget}/`. Both hosts'
GPUs idle.

Health check after first rows (2026-07-18): both workers alive, containers
`lcacmp_blue_*` up on both GPUs, GPU util 39-96%, VRAM ~4.0 GiB per GPU
(well under the 24 GiB ceiling; historical L<=2048 B4 SD peaks
were <= ~14 GiB). CSV headers written under
`out/lca_cmp/calibration/<family>/L<len>/`. Post-relaunch confirmation: the
first three token rows completed DONE with `.done` markers and complete
schema-fixed CSVs (`training.seed_applied=True`,
`train/peak_vram_mib=2680.6` at L1024/B4, val_acc 0.77/0.62/0.75 for seeds
0/1/2 — already above the 0.5 chance line at 2000 updates). First full pass
was stopped and relaunched within minutes after the CSV schema gap above was
found; no pre-fix CSV is retained as a result. Final completion check
recorded above (2026-07-18 19:23 pass complete).

## MRP-2 Natural AR-Hit Launch State

Checkpoint inventory found no compatible registered MRP-2 final checkpoints in
local, blue-demon, or Lizmark artifact trees. The active plan is therefore
targeted checkpoint retraining for the registered rows before AR-hit
evaluation.

| Host | GPU | Container/PID | Row | Log | State |
|---|---:|---:|---|---|---|
| blue-demon `~/set-attention-anchor-span-sync` | 1 | exited before post-shutdown audit | checkpoint retrain queue, rows token/b0/b25/b100 seeds 0-2 | `logs/mrp2_ar_hit_retrain_20260709_082300.log` | COMPLETE: 12/12 rows have epoch-10 CSV endpoints and final checkpoints |

The earlier log `logs/mrp2_ar_hit_retrain_20260708_175000.log` is a failed
launch-environment attempt with no HF cache mount; it produced no checkpoint
and no training endpoint. Do not use it as a scientific result.

Next MRP-2 atomic action is the registered AR-hit evaluation over the completed
checkpoints, not retraining.

The completed Lizmark watcher cleaned the legacy registry, deployed tested
source, and launched the corrected queue. An unrelated process was then found
using both GPUs. Corrected `b0` seed 2 and partial seed 3/4 plus `b25` seed 1
were quarantined outside the live tree; seeds 0 and 1 completed before the
foreign process started and remain accepted.

The final launcher requires `REQUIRE_EXCLUSIVE_GPU=1` and
`ALLOW_GPU_CORESIDENCY=0`. It checks occupancy before config/container setup
and immediately before `docker run`; occupied or unqueryable devices defer.
All co-resident partials and their admission telemetry are archived outside
the live tree.

The competing `cancer_rl_agent` container is stopped. Its partial outputs are
checksummed under
`/app/_gpu_handoff_archive/20260702_set_attention_priority`. Restart watcher
PID `3049751` was stopped on 2026-07-04: the current driver can exit with
diagnostic-retry rows, so the external workload must remain deferred until the
replacement wave is strictly validated.
The strict dry run reported 133 plans, 2 accepted skips, and 0 OOM skips;
launcher/handoff tests passed 6/6. At the single health check, admission saw
no prior CUDA processes, both GPUs contained only one set-attention process,
and both OOM registries were header-only.
Incident: `audit/incident_lizmark_gpu_contention_20260702.md`.

The final corrected MRP-1 snapshot contains 255 strict endpoint-valid full-data
rows. The registered selection island `L2048,B4` is complete and freezes
`b*=b25`; see `audit/SD_dense_paper5_final_20260708.md` and
`docs/sd_dense_paper5_matrix.md`.

MRP-2 and MRP-3 are no longer approval-gated: MRP-2 checkpoint retraining is
complete and awaits registered AR-hit evaluation, while MRP-3 is complete and
null. MRP-lca-cmp is now active as a separate long-context aggregation branch;
it is not an authorization to reopen MQAR or MRP-4. MRP-5 remains blocked until
MRP-2 completion and explicit transfer approval. Do not launch B2, landmark,
sparse, fixed-k, SD-10a, SD-11, MRP-4, or MRP-5 from this tracker alone.
