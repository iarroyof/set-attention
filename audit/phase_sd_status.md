# Set-Dictionary Branch Status Tracker

Last updated: 2026-07-07 after launching the Lizmark L3584/B4 replacement wave.

Scope: current experimental state for `set-dictionary/anchor-span`.

Shared-tracker write owner: the MRP-1 experiment-operations role while the
paper5 queues remain active. Concurrent task agents update their own
subplans/audits and submit a handoff; they do not edit this file. After MRP-1
closes, a replacement program-integration owner must be explicitly named here
before another agent edits the tracker.

## Canonical precedence

Read these files in order for current work:

1. `docs/set_dictionary_research_main_plan.md` for program scope and dependencies
2. this tracker for live state
3. the assigned task under `docs/agent_plans/`
4. `docs/sd_dense_paper5_matrix.md` for active MRP-1 cells
5. `docs/set_dictionary_dev_agent_prompt.md` for standing implementation guards
6. `docs/set_dictionary_model_provenance_for_math_agent.md` for current architecture/math provenance

`audit/phase_sd_status_legacy_through_20260625.md` and
`audit/SD_9_7_handoff_landmark_legacy_20260625.md` are provenance snapshots, not launch guidance.

Documentation audit: `audit/canonical_documentation_audit_20260630.md`. The local workspace is
canonical. Final propagation of the archive layout/latest wording to remote mirrors is pending; remote
agents must not prefer their local prose over this tracker/matrix.

## Active policy

- The active matrix is **exact dense only**: dense set-dictionary versus matched dense token attention.
- Coverage-scaled landmark (`landmark_coverage=0.25`) is removed from the active matrix. Its completed
  artifacts are historical quality/reference evidence only and must not support linear/sub-quadratic
  efficiency claims.
- Do not launch landmark, sparse, fixed-k, SD-10a, or SD-11 work without a new explicit user decision.
- Full WikiText-2 only: no `data.limit`; 10 epochs; applied RNG seeds 0--4 for corrected trainable paper cells; `D=384`,
  `d_ff=1536`, 6 layers, 8 heads, LR `1e-4`, warmup metadata `1000`.
- Regular paper blur rows: `{b0,b25,b50,b75,b100}`. Existing b62 rows are exploratory and excluded
  from the five-seed queue.
- Set guards: exact backend, `output_residual_mode=anchor_span`, `token_mlp.enabled=false`,
  `anchor.enabled=false`, CE-only, `candidate_fiber=endpoint_window`, no re-read/all_past/multivector.
- Corrected rows require `training.experiment_contract=sd_grid_seeded_v1`,
  `training.diagnostics_contract=current_matrix_v1`, applied seed equality,
  and complete family-specific diagnostics.
- After a detached launch, perform one compact health check and stop polling until the user requests
  another status check.

## Registered continuation program

The post-matrix research program is now canonical at
`docs/set_dictionary_research_main_plan.md`. It divides work into restart-safe
subplans under `docs/agent_plans/`:

| Workstream | State | Operational rule |
|---|---|---|
| MRP-0 reproducibility/checkpoint platform | PASS | Blue container validation passed on 2026-07-07: focused suite, duplicate strict token/b25 smokes, checkpoint replay, and eval-only immutability |
| MRP-1 five-seed exact-dense closure | REPLACEMENT RUNNING | Blue 120/120 complete; Lizmark first pass complete; 15 L3584/B4 mixed endpoint-diagnostic replacements launched; `b*=b25` frozen |
| MRP-2 natural AR-hit evaluation | BLOCKED | requires MRP-0 PASS, frozen MRP-1 `b*`, and explicit approval |
| MRP-3 synthetic MQAR | generator/trainer infrastructure READY; launches BLOCKED | Blue container tests, dry-run, guard, and tiny smoke passed; launches require MRP-1 completion and explicit approval |
| MRP-4 larger scale separation | HOLD | deterministic MRP-3 trigger plus separate explicit approval |
| MRP-5 tokenizer-matched WT2/PG-19 | BLOCKED | requires MRP-0, MRP-1, MRP-2, MRP-3 review, and explicit approval |
| MRP-6 theory/proofs | PASS for analytic theory package | MRP-6A/B/C focused tests passed in Blue container; MRP-6D canonical TeX integration and clean build passed; MRP-3-dependent specialization remains conditional |
| MRP-7 paper synthesis | BLOCKED | waits for empirical and theory gates |

No new experimental queue is authorized by the plan alone.

## Open reproducibility incident

`audit/incident_training_seed_not_applied_20260630.md` records that the legacy
runner logged `training.seed` without applying it. All legacy MRP-1 rows are
unpaired stochastic replicates, not reproducible seeds.

The runner now applies the configured seed before data/model construction and
logs requested/applied/torch seeds plus deterministic and benchmark flags.
The isolated corrected namespace refuses completion without matching
provenance. The seed/config/diagnostics patch is deployed on both experiment
hosts. At the time of the corrected MRP-1 launch, full MRP-0 remained
incomplete because checkpoint, loader-batch, and data/tokenizer provenance
work had not been implemented.

The 2026-07-06 implementation audit confirmed this was not merely pending
validation at launch time: the checkpoint and ordered-token modules did not exist, no
corrected MRP-1 checkpoints were saved, and all 120 Blue logs warn that
CuBLAS execution was not fully deterministic. This does not erase the applied
five-seed PPL/VRAM matrix, but exact replay and downstream checkpoint reuse are
not supported. See `audit/incident_mrp0_prelaunch_gap_20260706.md`.

The missing platform has since been implemented locally, with focused tests
and a strict duplicate token/b25 replay validation script. It remains
unvalidated in the project container because the Blue sync attempt was
blocked by the execution environment's remote-action usage limit. No
selected retraining is authorized until that PASS is recorded. Audit:
`audit/MRP_0_reproducibility_platform.md`.

## Grid identity and diagnostics incident

`audit/incident_sd_grid_identity_diagnostics_20260701.md` records scanner
conflation, missing per-group training diagnostics, and the strict-contract LR
type mismatch. No duplicate successful result was found. Blue partial/fail-fast
attempts and superseded retry records are outside aggregation roots; see
`audit/SD_grid_duplicate_cleanup_20260701.md`.

The final blue protocol now:

- validates set and token configs through the real runner before workers start;
- rejects every non-null data limit and wrong implementation/config;
- logs fine/coarse routing, pooling, router parameter/gradient, set-stack
  gradient, ablation, effective-range, and rarity/position diagnostics;
- fails corrected aggregation on duplicate cell IDs or missing diagnostics.

The local launcher additionally stops on the first contract error. It must not
be synchronized into active blue/lizmark source; deploy after each host is idle.

## Completed architecture ladder

| Range | Status | Current interpretation |
|---|---|---|
| SD-1--SD-4 | DONE | Causality, leakage, fairness, and config contracts validated. |
| SD-5--SD-8.1 | DONE | Capacity/anchor-span ladder validated; historical details remain in the archived tracker and per-task audits. |
| SD-9 short dense | DONE | Multi-resolution set-vs-set signal established at `L=512,B=16`; 25% blur beats all-fine on PPL and VRAM. |
| SD-9/9.5/9.6 landmark arms | HISTORICAL | Preserve artifacts, but exclude from the active dense claim and from linear-efficiency conclusions. |
| SD-10a/SD-11 | HOLD | No architecture changes until the dense matched comparison is complete and reviewed. |

## Completed primary three-seed matrix

Active scheduler: `scripts/run_sd_grid.sh`.
Canonical scanner: `scripts/sd_grid_status.py`.

| Island | Dense set status | Dense token status |
|---|---|---|
| exact, `L=512,B=16` | six blur rows, 3/3 seeds complete | seeds 0--2 complete at registered epoch 10 |
| exact, `L=512,B=4` | six blur rows, 3/3 seeds complete | seeds 0--2 complete |
| exact, `L=1024,B=4` | six blur rows, 3/3 seeds complete | seeds 0--2 complete |
| exact, `L=2048,B=4` | six blur rows, 3/3 seeds complete | seeds 0--2 complete |
| exact, `L=4096,B=4` | b50/b62/b75/b100 complete; b0/b25 OOM 3/3 | OOM 3/3 before epoch 1 |

Dense set endpoint: 84/90 planned cells completed and 6/90 resolved as OOM. The set sweep is therefore
fully resolved. Through `L=2048`, b25 is the lowest-PPL row and also uses less VRAM than b0. At `L=4096`,
b0 and b25 do not fit on 49 GiB; b50 is the lowest-PPL feasible row.

## Primary job resolution

The primary corrected queues ended and were validated:

- Blue PID `3489171`: queue ended normally; all 8 requested cells completed 10/10.
- Lizmark PID `2153424`: queue ended normally; L2048 completed 3/3 and L4096 OOMed 3/3.
- Corrected artifacts/logs were pulled locally and validated.
- Result audit: `audit/SD_dense_matched_results.md`.

## Configuration incident

The first exact-token attempts failed before model construction because the scheduler used the landmark
token YAML and attempted to clear inherited `backend_params` with `{}`. Exact validation correctly
rejected the nonempty parameters.

Resolution:

- exact token cells now use `configs/paper_lr_norm/baseline_dense_exact.yaml`;
- landmark and sparse token configs are selected only for those explicit backends;
- in-flight duplicate detection uses the same backend-native token config signature;
- `scripts/sd_grid_status.py` reports the registered target epoch (10 by default), so a reusable 30-epoch
  run contributes its epoch-10 row rather than its final row;
- container config checks and token-only dry runs passed on both hosts.

Impact: no set result was affected. The corrected controls are now complete and establish the matched
verdict in `audit/SD_dense_matched_results.md`.

Incident: `audit/incident_sd_dense_token_exact_backend_params.md`.

## Frontier extension resolution

The seed-0 full-data frontier extension tested the strong L4096/B4 b50 result with an overlap
rectangle that separates length and native batch:

- blue: L2048/B3 full blur + token;
- lizmark: L3584/B4, L3584/B3, and L4096/B3 full blur + token;
- B2 is fallback only if B3 remains unsupported.

See `docs/sd_dense_matched_comparison_plan.md`. Do not reopen landmark or add
B4 OOM retries to the active matrix.

Final frontier state:

| Host | PID | Cells | Log | Result |
|---|---:|---|---|---|
| blue-demon | 3657887 | L2048/B3 full blur + token, seed 0 | `logs/sd_grid_blue_frontier_seed0.log` | DONE 7/7, exit 0, validated and pulled |
| lizmark | 2185668 | L3584/B4, L3584/B3, L4096/B3 full blur + token, seed 0 | `logs/sd_grid_lizmark_frontier_seed0.log` | DONE 21/21, exit 0, validated and pulled |

All 28 cells use full data and 10 epochs. Validation found no guard, metadata, peak-VRAM, NaN/Inf,
traceback, or unexpected OOM failure. Both hosts and all four GPUs are idle. B2 remains unlaunched.

Seed-0 selected results:

- L2048/B3: b25 `900.751/13797.1 MiB`; token `885.786/14189.7 MiB`.
- L3584/B4: b25 `846.937/38552.9 MiB`; token `890.875/41076.2 MiB`.
- L3584/B3: b62 `915.146/24467.7 MiB`; token `869.081/31035.0 MiB`.
- L4096/B3: b25 `840.564/35350.1 MiB`; token `969.223/37955.4 MiB`.

Native B3 changes optimization and remains a separate island. The two seed-0 Pareto results are
promising but not conclusive. Audit: `audit/SD_dense_frontier_extension.md`.

## Five-seed paper matrix

The 2026-06-30 queues are retained as legacy unseeded-replicate evidence and
have ended. Corrected queues are active on both hosts.

The corrected matrix reruns every supported row with actual seeds `0..4`.
Every supported island uses set rows `{b0,b25,b50,b75,b100}` plus exact token;
b62 remains outside the regular matrix.

| Host | Driver PID | Corrected cells | Queue log | State |
|---|---:|---:|---|---|
| blue-demon | exited normally | 120/120 endpoint-valid done | `logs/sd_grid_blue_paper5_seeded_v1.log` | COMPLETE and locally validated; both GPUs idle |
| lizmark replacement | driver PID 3940226; workers 3940654/3940655 | 15 L3584/B4 mixed replacement rows planned; first two b25 rows active | `logs/sd_grid_lizmark_paper5_seeded_v1_replacements_20260707.log` | RUNNING EXCLUSIVE; GPUs ~41.9 GiB each at launch check |

The 2026-07-07 Lizmark check found no active grid driver or training process;
both RTX 6000 Ada GPUs reported 1 MiB used and 0% utilization. The queue log
ends with `=== SD-GRID lizmark complete ===`. Artifacts were pulled locally.

The completed Lizmark watcher cleaned the legacy registry, deployed tested
source, and launched the corrected queue. An unrelated process was then found
using both GPUs. Corrected `b0` seed 2 and partial seed 3/4 plus `b25` seed 1
were quarantined outside the live tree; seeds 0 and 1 completed before the
foreign process started and remain accepted.

The final launcher requires `REQUIRE_EXCLUSIVE_GPU=1` and
`ALLOW_GPU_CORESIDENCY=0`. It checks occupancy before config/container setup
and immediately before `docker run`; occupied or unqueryable devices defer.
All co-resident partials and their admission telemetry are archived outside
the live tree.

The competing `cancer_rl_agent` container is stopped. Its partial outputs are
checksummed under
`/app/_gpu_handoff_archive/20260702_set_attention_priority`. Restart watcher
PID `3049751` was stopped on 2026-07-04: the current driver can exit with
diagnostic-retry rows, so the external workload must remain deferred until the
replacement wave is strictly validated.
The strict dry run reported 133 plans, 2 accepted skips, and 0 OOM skips;
launcher/handoff tests passed 6/6. At the single health check, admission saw
no prior CUDA processes, both GPUs contained only one set-attention process,
and both OOM registries were header-only.
Incident: `audit/incident_lizmark_gpu_contention_20260702.md`.

The frozen 2026-07-07 snapshot contains all 255 first-pass full-data rows.
Strict endpoint validation accepts 240 rows and still rejects the 15 mixed
`L3584,B4` rows because their endpoint gradient diagnostics are `NA`. Their
PPL/VRAM is provisional only. Snapshot:
`audit/SD_dense_paper5_partial_20260707.md` and
`out/paper_integrated_evidence/checks/sd_grid_seeded_v1_post_lizmark_20260707/`.
Incident: `audit/incident_sd_grid_epoch_gradient_probe_cadence_20260704.md`.

Replacement launch audit: `audit/SD_dense_paper5_replacements_20260707.md`.
Before launch, `cancer_rl_agent__deferred_until_sd_grid_release` was stopped
again, the 15 invalid first-pass records and markers were archived outside the
corrected root, strict status accepted 120 lizmark rows, and the dry run
reported exactly 15 plans plus 120 skips.

Complete-valid b25 now mean-dominates token at `L2048/B4`, `L3584/B3`, and
`L4096/B3`; the PPL intervals still cross zero. The provisional `L3584/B4`
b25 row also mean-dominates token but remains replacement-gated.

The registered selection island `L2048,B4` is complete and freezes
`b*=b25`; see `audit/SD_dense_paper5_results.md`. MRP-0 passed Blue
container validation on 2026-07-07. MRP-2/3/5 remain blocked by MRP-1
closure and explicit launch-approval gates.

The superseded 08:44 reduced-row-selection queues were stopped before any new run completed. No valid
existing artifact was deleted. Corrected host dry runs reported exactly 58/90 pending and zero b62
cells.

Matrix and comparison contract: `docs/sd_dense_paper5_matrix.md`.

Next atomic action: stop polling until user requests status or reports
completion. Then pull the replacement artifacts and rerun the strict scanner.
Do not launch B2, landmark, sparse, fixed-k, effective-B4 accumulation,
SD-10a, SD-11, MRP-2, MRP-3, or MRP-5 while this replacement gate remains
open.
