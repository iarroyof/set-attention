# Deprecated Configs

These configs are retained for historical reference only. They must not be used
by active revision grids or launch scripts.

- `wikitext2_nystrom.yaml`: Nyström backend example moved out of the active
  config tree for A1.10. Nyström is deprecated for this revision cycle; use the
  landmark backend with `model.backend_params.landmark_coverage`.
